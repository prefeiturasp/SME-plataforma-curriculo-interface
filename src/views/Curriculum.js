import React, { Component } from 'react';

class Curriculum extends Component {
  render() {
    return (
      <section>
        <h1>Currículo</h1>
      </section>
    );
  }
}

export default Curriculum;
